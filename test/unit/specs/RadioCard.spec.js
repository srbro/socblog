//
// This card does not exist use for card/componets/Cards/CardRadio maybe
//

// import Vue from 'vue'
// import RadioCard from 'src/containers/sections/Radio/RadioCard'

// describe('RadioCard.vue', () => {
//   const propsData = {
//     event: {
//       logoUrl: 'https://placehold.it/350x150',
//       progress: 0.5,
//       firstRowText: 'firstRowText',
//       secondRowText: 'secondRowText'
//     },
//     selected: true,
//     showDetails: true
//   }
//   const Ctor = Vue.extend(RadioCard)
//   const vm = new Ctor({ propsData }).$mount()

//   it('should event have firstRowText field', () => {
//     expect(vm.$el.querySelector('.first-row').textContent).to.equal(propsData.event.firstRowText)
//   })
//   it('should event have secondRowText field', () => {
//     expect(vm.$el.querySelector('.second-row').textContent).to.equal(propsData.event.secondRowText)
//   })
//   it('should event have logoUrl field', () => {
//     expect(vm.$el.querySelector('.logo').getAttribute('src')).to.equal(propsData.event.logoUrl)
//   })
//   it('should have active class if showDetails is true', () => {
//     expect(vm.$el.querySelector('.detail-container').classList.contains('active')).to.equal(propsData.showDetails)
//   })
//   it('should have is-selected class if selected is true', () => {
//     expect(vm.$el.classList.contains('is-selected')).to.equal(propsData.selected)
//   })
// })
