// import Vue from 'vue'
// import CardSettings from 'src/components/CardSettings'

// describe('CardSettings.vue', () => {
//   const propsData = {
//     icon: 'settings-about',
//     text: 'About',
//     selected: true
//   }
//   const Ctor = Vue.extend(CardSettings)
//   const vm = new Ctor({ propsData }).$mount()

//   it('should give special is-selected class if selected is true', () => {
//     expect(vm.$el.classList.contains('is-selected')).to.equal(propsData.selected)
//   })
//   it('should display text', () => {
//     expect(vm.$el.querySelector('.text').textContent).to.equal(propsData.text)
//   })
// })
